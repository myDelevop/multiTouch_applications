package wwf.ClassificationGame.util;

import java.io.File;

public class FilesPath {

	public final static String rootPath = System.getProperty("user.dir") + File.separator + "data" + File.separator
			+ "cg" + File.separator;

	public final static String rootFont = "cg" + File.separator + "fonts" + File.separator;

	public final static String rootInformation = "cg" + File.separator + "information" + File.separator;

	public final static String rootGame = "cg" + File.separator + "game" + File.separator;

	public final static String rootCategorie = "cg" + File.separator + "game" + File.separator + "categorie"
			+ File.separator;
}
